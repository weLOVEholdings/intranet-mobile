import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  logo_icon: {
    resizeMode: 'center',
    width: 200,
    height: 100,
  },
  container: {
    // flex: 1,
    alignItems: 'center',
    backgroundColor: 'white',
  },
});
